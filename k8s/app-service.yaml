apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: licitaciones-namespace
spec:
  type: ClusterIP
  selector:
    app: licitaciones-app
  ports:
    - port: 8080
      targetPort: 8080
      name: http